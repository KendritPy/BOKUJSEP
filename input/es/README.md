# Spanish runtime ISO

`scripts/setup.ps1` creates `Boku_ES.iso` here by applying the public
GriffithVIII/TraduSquare Spanish v1.0 xdelta patch to the clean Japanese ISO.

Do not distribute the generated ISO. The patch archive and generated image are
both ignored by Git.
